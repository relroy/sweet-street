<div class= "container-fluid">
  <h1>Banners</h1>
  <% @layouts.each do |l| %>
  
  <a href="/layouts/<%= l.id %>/edit" ><p><img src= '<%= l.photo_url %>' height="90" width="100%" ></p></a>
  
  <% end %>
</div>